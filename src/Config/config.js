const API_BASE_URL = "https://backend.photographercompany.com";

const API_URLS = {
  GET_DASHBOARD_DETAILS: (clientId) => `${API_BASE_URL}/clients/${clientId}`,
  GET_CLIENT_DETAILS: (clientId) => `${API_BASE_URL}/clients/${clientId}`,
  GET_CLIENT_DETAILS: (clientId) => `${API_BASE_URL}/clients/${clientId}`,
  GET_CLIENT_DETAILS: (clientId) => `${API_BASE_URL}/clients/${clientId}`,


  LOGIN: (clientId) => `${API_BASE_URL}/admin/signin`,
};

export default API_URLS;
